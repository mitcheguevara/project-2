<h2><%= @event.name %></h2>

 <%= form_for [@owner, @event] do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>
 <br>
 <%= f.label :location %>
 <%= f.text_field :location %>
 <br>
 <%= f.label :event_img %>
 <%= f.text_field :event_img %>
 <br>
   <%= f.submit "Update" %> <%= link_to "Cancel", owner_event_path(@owner, @event) %>
 <% end %>

<%= link_to 'Its not happening', owner_event_path(@owner, @dog),
            data: {:confirm => 'GoodBye'}, :method => :delete %>
